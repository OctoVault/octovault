---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: octovaults.octovault.it
spec:
  group: octovault.it
  names:
    kind: OctoVault
    listKind: OctoVaultList
    plural: octovaults
    shortNames:
    - ov
    - ovl
    singular: octovault
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.octoRepositoryRef.name
      name: OctoRepositoryRef
      type: string
    - jsonPath: .spec.repository
      name: Repo
      type: string
    - jsonPath: .spec.observedRef
      name: Ref
      type: string
    - jsonPath: .status.resolvedType
      name: Type
      type: string
    - jsonPath: .spec.targetName
      name: Target
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.observedRevision
      name: Rev
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OctoVault is the Schema for the octovaults API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OctoVault
            properties:
              gitRef:
                pattern: ^[A-Za-z0-9._\/\-]+$
                type: string
              octoRepositoryRef:
                description: GitHub 자격 증명 소스 (OctoRepository)
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              path:
                description: values.yaml 위치 ("path/to/values.yaml")
                minLength: 1
                pattern: ^.*\.ya?ml$
                type: string
              pollInterval:
                default: 1m
                description: '폴링 간격 분 단위 (Default: 1m)'
                pattern: ^([0-9]+(m))+$
                type: string
              repository:
                description: '저장소 이름: "org/repo" 사용'
                pattern: ^([A-Za-z0-9_.-]+)(\/[A-Za-z0-9_.-]+)?$
                type: string
              targetName:
                description: 적용 대상 리소스 이름/네임스페이스
                minLength: 1
                type: string
              targetNamespace:
                type: string
            required:
            - octoRepositoryRef
            - path
            - repository
            - targetName
            type: object
          status:
            description: status defines the observed state of OctoVault
            properties:
              appliedDataHash:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              externalRefs:
                description: 외부 참조들의 현재 버전/스테이지 정보
                items:
                  description: ExternalRefStatus 외부 소스(AWS SM 등)의 버전/스테이지 정보를 표시
                  properties:
                    key:
                      description: Key OctoVault values.yaml의 data.key (어떤 키가 이 외부
                        ref를 사용했는지)
                      type: string
                    name:
                      description: Name 외부 시크릿 식별자
                      type: string
                    provider:
                      description: 'Provider 예: "AwsSecretsManager"'
                      type: string
                    versionID:
                      description: VersionID 현재 사용된 버전
                      type: string
                    versionStages:
                      description: VersionStages 예) ["AWSCURRENT"]
                      items:
                        type: string
                      type: array
                  type: object
                type: array
              externalRefsSummary:
                description: 출력용 요약 문자열 (printer column)
                type: string
              lastSyncedTime:
                format: date-time
                type: string
              message:
                type: string
              observedRef:
                type: string
              observedRevision:
                type: string
              phase:
                type: string
              resolvedType:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
